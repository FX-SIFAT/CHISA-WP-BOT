module.exports = { name: "readviewonce", aliases: ["rvo"], category: "danger", code: async (ctx) => { if (!tools.cmd.checkQuotedMedia(ctx.quoted?.messageType, ["audio", "image", "video"])) return await ctx.reply(tools.msg.generateInstruction(["reply"], ["viewOnce"])); const quotedMessage = ctx.quoted.message; if (!quotedMessage[ctx.quoted.messageType].viewOnce) return await ctx.reply(tools.msg.generateInstruction(["reply"], ["viewOnce"])); try { delete quotedMessage[ctx.quoted.messageType].viewOnce; await ctx.sendMessage(ctx.id, { ...quotedMessage, raw: true }); await ctx.reply(tools.msg.info("View-once media has been sent!")); } catch (error) { await tools.cmd.handleError(ctx, error); } } };